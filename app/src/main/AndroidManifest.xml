<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <!-- Permissão para enviar notificações no Android 13+ -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

  <application

      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:theme="@style/Theme.Teste1"
      tools:targetApi="33"> <!-- Atualizado para Android 13 -->

    <!-- Tela Principal -->
    <activity android:name=".View.NotasActivity"/>

    <activity
        android:name="com.example.teste1.View.MainActivity"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- Tela de Avaliações -->
    <activity android:name=".com.example.teste1.View.ThirdActivity"/>
    <activity android:name=".com.example.teste1.View.EditPesoActivity"/>
    <activity android:name=".View.NotaDetalheActivity" />




    />
  </application>

</manifest>
